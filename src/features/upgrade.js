export class Upgrade {
  constructor({ name, canAfford, desc, costDesc }) {
    this.name = name;
    this.canAfford = canAfford;
    this.desc = desc;
    this.costDesc = costDesc;
  }
  buy() {}
  effect() {}
}
// ah yes TextDecoderStream
// LMAO
